---
- name: Converge
  hosts: all
  gather_facts: true
  become: true
  vars:
    tailscale_auth_key: ""
    tailscale_up: false
  tasks:
    - name: Include tailscale role
      ansible.builtin.include_role:
        name: tailscale
