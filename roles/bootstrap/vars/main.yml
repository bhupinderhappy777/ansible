---
# vars file for roles/bootstrap
---
# The user you want to create and manage
bootstrap_user: bhupi

# Detect if we need 'wheel' (RedHat/Fedora) or 'sudo' (Debian/Ubuntu)
admin_group: "{{ 'wheel' if ansible_facts['os_family'] == 'RedHat' else 'sudo' }}"

# Your public SSH key for passwordless entry
ssh_pub_key: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKCR+YCc9AluIbnedoE0Ied8cG0CpnQBh2Mf4lYR71/x"
