- name: Install base packages
  ansible.builtin.package:
    name: "{{ item.pkg }}"
    state: present
  loop: "{{ common_system_packages[ansible_facts['os_family']] | default(common_system_packages['Debian']) }}"
  loop_control:
    label: "{{ item.pkg }}"
