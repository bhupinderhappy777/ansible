---
- name: Ensure sysstat service is enabled and started
  ansible.builtin.service:
    name: sysstat
    state: started
    enabled: true
  become: true

# Removed redundant Zsh tasks. Zsh configuration is handled in roles/zsh.
